---
interface Props {
  title?: string;
}
const { title = "Título por defecto" } = Astro.props as Props;
---

<div class="flex flex-col w-full mt-3">
  <h2 class="text-center text-2xl font-semibold text-primary w-full">{title}</h2>
  <slot />
</div>